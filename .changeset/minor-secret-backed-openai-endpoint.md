---
"gh-aw": minor
---

Add `sandbox.agent.targets.openai.base-url-secret` for Codex workflows so AWF can route OpenAI-compatible traffic through a runtime secret-backed endpoint without committing the endpoint host to lockfiles or artifacts.
